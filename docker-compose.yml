version: '3'

services:
  db:
    image: postgres
    volumes:
      - ./pgdata/:/var/lib/postgresql/data
  web:
    build:
      context: .
      dockerfile: ./Dockerfile
    command: /code/start.sh
    ports:
      - "8000:8000"
    depends_on:
      - db    